# Grafana datasource provisioning (ADR-013).
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: dossier-prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
